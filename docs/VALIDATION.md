# 검증 기록

[README로 돌아가기](../README.md)

검증일: **2026-09-22**  
기준 코드: `64b77351f1670f939af6cd861e1f4245b4a79447`  
환경: Windows, Python 3.12.10, Flask 3.1.3, OpenAI SDK 3.17.0

| 확인 항목 | 결과 |
| :--- | :--- |
| 메뉴 `/` | HTTP 200 |
| 자세 입력 `/posture` | HTTP 200 |
| 운동 강도 입력 `/intensity` | HTTP 200 |
| 식단 입력 `/diet` | HTTP 200 |
| `/calculate` 로컬 계산 | HTTP 200, 예시 입력에서 `52.50 kg` 출력 |
| 실제 자세 분석·식단 AI 호출 | 미실행 |
| 기존 연구 성능표 재산출 | 미실행 |

계산 확인 입력: 남성, 체중 70kg, 키 175cm, 골격근량 30kg. 위 결과는 코드의 계산 동작을 확인한 것이며 중량 추천의 의학적 타당성을 검증한 것은 아닙니다.

검증 서버는 `AiTrainer_flaskServer` 폴더에서 `python -m flask --app app run --host 127.0.0.1 --port 5000`으로 실행했습니다. 소스 변경 없이 디버그 모드를 끈 로컬 서버에서 화면과 계산 경로를 확인했습니다.

외부 AI API를 호출하지 않았으므로 실제 모델 응답, API 권한, 지연·비용, SDK와 모델 호출의 전체 호환성은 검증하지 않았습니다. 기존 서비스 캡처와 성능표는 개발 당시의 기록으로 구분했습니다.
